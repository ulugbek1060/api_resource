import 'package:api_resource/base_api_resource.dart' as api_resource;

// Usage:
// void main() async {
//   int currentPage = 1;
//   List<Post> posts = await fetchPosts(currentPage, 10);
//   print('Fetched ${posts.length} posts from page $currentPage');
// }
